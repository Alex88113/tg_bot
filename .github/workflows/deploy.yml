name: Auto Deploy

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger deploy via webhook
      run: |
        curl -X POST http://91.215.250.220:8080/deploy
